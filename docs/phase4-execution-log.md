# 4단계 실행 로그 (Phase 4 Execution Log)

> 4단계 종합실행계획서(AX_P4_Master_Plan.docx) 이행 기록 — odooaierp.com 웹앱
> 서비스화. 원칙: 실측 없이 완료 표기하지 않는다. 문제는 P4-I 번호로 남긴다.
> 이 환경의 한계: Cloudflare 계정·실서버 접근이 없으므로 Tunnel '실결선'은
> 사용자 실행 준비물(스크립트+가이드)로 완성한다 — 정직하게 구분해 표기.

## 사용자 결정 (2026-09-11 접수)

| 결정 요청 | 결정 |
|---|---|
| ① 체험 서버 위치 | **보유 서버** — P4-7 가이드는 보유 앱 서버 기준으로 작성 |
| ③ 체험 신청 방식 | **초기 승인제** — 신청 폼 → 관리자 승인 → 계정 발급(P4-5에 반영) |
| ② 서브도메인 3종 | 미결 — try/app/status.odooaierp.com 안대로 준비물 작성, 승인 시 즉시 적용 |
| ④ 개인정보 처리방침 법률 검토 | 미결 — P4-8은 법률 검토 전 초안으로 작성 |

## 진행 현황

| 항목 | 12주 계획 위치 | 상태 | 실측 근거 |
|---|---|---|---|
| P4-1 체험 테넌트 도구 (생성·리셋·파기·업로드 파기) | W1·W5 | **완료** | 실 T사 템플릿(119MB) 생성 0초·로그인·승인함 렌더, 테스트 5종 |
| P4-2 웹앱 공개 보강 (로그인 제한·잠금·워터마크) | W2 | **완료** | 실패 5회→15분 잠금(423)·성공 시 소거·체험판 워터마크, 테스트 4종 |
| P4-3 웹 업로드 화면 (엑셀/POS) | W4 | **완료** | /upload 3유형 반입·멱등·권한·needs_mapping 실측, 테스트 3종 |
| P4-4 업타임 하트비트·경보 + status | W2·W11 | **완료** | 다운 crit 1회·복구 info 1회 실측, /health·/status 공개, systemd 타이머 |
| P4-5 랜딩 개편 (체험 신청) | W3 | **완료(재배포 대기)** | #trial 승인제 섹션+nav, 스크린샷 QA — Cloudflare 재업로드는 사용자 |
| P4-6 Odoo 연결 마법사 1차 | W8~9 | **완료** | 실 Odoo 17로 실검증 — 버전·모듈 71·wal_level·권한·11테이블·기존 발행 감지 |
| P4-7 Tunnel 결선 스크립트·가이드 | W1 | **완료(준비물)** | install-tunnel.sh(문법 검증)+deploy-odooaierp.md — 서버 실행만 남음 |
| P4-8 약관·개인정보 처리방침 초안 | W10~11 | **완료(초안)** | terms-draft.md·privacy-draft.md — 게시 전 법률 검토 필요 |

## P4-1: 체험 테넌트 도구 (2026-09-11)

`core/axp/tenant.py` + CLI `python3 -m axp.cli tenant create|reset|destroy|purge-uploads|list`.

- **create**: 템플릿(기본: 합성 T사 데이터 루트) 복제 → 프로파일 개명(trial 플래그)
  → 체험 계정 무작위 비밀번호 발급(0600, 반환값·목록에 비밀번호 노출 금지).
  실측: 119MB 템플릿 복제 1초 미만, 새 계정 로그인→승인함 렌더 확인.
- **reset**: 템플릿 재복제(개명 유지) — 매일 0시 초기화 크론용.
- **destroy**: 디렉터리 + **PG 스키마 동반 파기**(P3-I7 재발 방지 내장,
  식별자 정규식 검증 — P3-I8 LIKE 함정 회피).
- **purge-uploads**: raw/ 아래 시효(기본 24h) 지난 업로드 원본 삭제 —
  4단계 정직 조항("체험 파일 24시간 내 파기")의 구현체. 25h/신규 파일로 실측.
- 테스트 5종(개명·계정·중복/이름 규칙 거절, 리셋 오염 제거, 파기, 시효 파기,
  목록 비밀번호 은닉).

## 문제 기록 (P4-I)

| 번호 | 문제 | 처리 |
|---|---|---|
| P4-I1 | 템플릿에 묻어온 웹 계정(axp_users)이 새 테넌트에 복사돼 부트스트랩이 건너뜀 — 새 비밀번호가 통하지 않음(401 실측) | create/reset에서 axp_users·axp_sessions 테이블 제거 → 부트스트랩 재발급 (**해결**) |

## P4-2: 웹앱 공개 보강 (2026-09-11)

- **로그인 시도 제한**: 실패 5회(AXP_LOCK_THRESHOLD) → 15분(AXP_LOCK_MINUTES) 잠금.
  잠금 중엔 올바른 비밀번호도 423으로 거절(실측). 존재하지 않는 계정도 동일
  동작 — 계정 존재 여부를 흘리지 않는다. 잠금 발생은 경보(warn)로 기록.
- **성공 시 카운터 소거** — 정상 사용자가 오타 몇 번으로 누적 잠금되지 않는다.
- **체험판 워터마크**: profile.trial=True면 전 화면 헤더에 '체험판 · 합성 데이터'
  상시 고지 + 회사명 표시(4단계 정직 조항 구현). 테넌트 도구의 trial 플래그와 연동.
- 테스트 4종 추가(잠금·유령 계정 동일 동작·소거·워터마크) + P4-I1 회귀 1종
  — 전체 65+2skip(sqlite)·67(PG) green.

## P4-3: 웹 업로드 화면 (2026-09-11)

- `/upload` (steward 전용) — 엑셀·POS 정산·POS 영수증 3유형 선택 업로드.
  cp949 CSV 실업로드→스테이징 적재→같은 파일 재업로드 '이미 반입' 멱등까지
  웹 경로로 실측. 크기 상한(기본 20MB)·안전 파일명·수신함(raw/webupload) 보존.
- 처음 보는 양식은 '처음 보는 양식' 안내 + 발견한 열 표시(needs_mapping UX).
- 반입 결과에 거절 행(한글 사유)·이중 집계 경고·격리 큐 링크 표시 —
  "가져와서 정리한다"가 화면 흐름이 됐다. 체험 고객의 '내 데이터 체험' 관문.
- 테스트 3종 — 전체 68+2skip(sqlite)·70(PG) green.

## P4-4: 업타임 하트비트·경보 + status 페이지 (2026-09-11)

- 웹앱 `/health`(DB 왕복 하트비트 표적)·`/status`(공개 상태 — 웹앱/DB/최근 24h
  심각 경보 신호만, 데이터·계정 미노출) 추가. 체험판 워터마크도 status에 표시.
- `deploy/heartbeat.py`: 연속 2회 실패 순간 crit 1회 발화, 복구 시 info 1회 —
  상태 파일로 중복 억제. **실측**: 죽은 포트로 다운 감지→재기동→복구 경보까지
  전 흐름 확인. systemd `axp-heartbeat.service/.timer`(5분) 동봉.
- SLA 3장 "P1은 플랫폼이 먼저 감지·경보"의 구현체. status.odooaierp.com은
  Tunnel 결선 시 이 /status를 그대로 노출하면 된다(P4-7 가이드에 포함).
- 테스트 1종(공개 접근·최소 정보) — 전체 69+2skip(sqlite)·71(PG) green.

## P4-7: Cloudflare Tunnel 결선 준비물 (2026-09-11)

이 환경에는 Cloudflare 계정·보유 서버 접근이 없으므로 **실결선이 아니라
사용자 실행 준비물**이다(정직 표기). 사용자 결정 '보유 서버' 반영.

- `deploy/install-tunnel.sh`: cloudflared 설치→로그인→터널 생성→
  try/app/status.odooaierp.com DNS 라우트→systemd 상시 기동을 한 번에.
  인바운드 개방 불필요(아웃바운드 터널). bash -n 문법 검증.
- `docs/deploy-odooaierp.md`: 반나절 실행 순서 — 스택 설치→체험 테넌트→
  Tunnel→운영 크론 3종(0시 리셋·24h 파기·하트비트)→승인제 신청 흐름→장애 대응.
  KST/UTC 크론 시각 명시, 롤백 절차 포함(기존 Workers 정적 사이트 무영향).

## P4-5: 랜딩 개편 — 체험 신청 (2026-09-11)

- odooaierp/index.html에 `#trial` 섹션 신설 — **승인제**(사용자 결정 ③) 3단계 안내:
  신청 메일(회사·담당자·이메일 서식 자동 채움) → 1영업일 내 계정 회신 →
  매일 0시 초기화·업로드 파일 24h 파기·합성 데이터 고지. nav에 '체험 신청' 추가.
- Playwright 스크린샷으로 렌더 QA. **주의: 정적 사이트 반영은 사용자가
  Cloudflare(Workers)에 재업로드해야 실사이트에 나타난다** — 기존 배포 방식 동일.

## P4-6: Odoo 연결 마법사 1차 (2026-09-11)

3단계에서 사람 손으로 실증한 절차(버전 확인→publication→검증)를 화면으로 —
`/connect` (admin 전용, 읽기 전용 접속, 비밀번호 미저장).

- 검사 항목: Odoo 버전(base.latest_version)·설치 모듈 수 · wal_level=logical 여부
  (아니면 조치 문구) · 계정 REPLICATION 권한 · CDC 후보 13테이블 존재 매트릭스 ·
  기존 axp_pub 발행 존재 여부.
- 결과 화면에서 **그 인스턴스 구성에 맞춘 발행 SQL을 생성**(존재 테이블만) —
  DBA가 복사·실행. 다음 단계(구독·매핑 뷰·정합)는 체크리스트 C로 연결.
- **실검증**: 로컬 실 Odoo 17(odoo_real)로 — 버전 17.0.1.3 · 모듈 71 ·
  wal_level=logical · REPLICATION 있음 · 11개 발행 가능 · 부재 2개 자동 제외 ·
  기존 axp_pub 감지(DROP 안내)까지 전부 표시 확인.
- 테스트 3종(admin 강제·접속 실패 UX·비Odoo DB 감지/PG 전용) —
  전체 71+2skip(sqlite)·74(PG) green.

## P4-8: 약관·개인정보 처리방침 초안 (2026-09-11)

- `ax-platform/docs/terms-draft.md`: 체험판 약관 — 승인제 계정, 매일 0시 초기화,
  업로드 24h 파기, 판단 카드는 제안(책임 제한), 14일 기간. 구현된 장치
  (잠금·파기 크론·워터마크)와 조문이 일치하도록 작성.
- `ax-platform/docs/privacy-draft.md`: 최소 수집 원칙 — 신청 3항목만,
  PII 컬럼 자동 차단(구현 기준 명시), 제3자 제공 없음, Cloudflare·자사 서버
  위탁 관계, 데이터 주권(고객 소유) 명시.
- 둘 다 **법률 검토 전 초안**(사용자 결정 ④ 미결) — 게시는 검토 후.

## P4-9: 온보딩 위저드 1차 — W6 (2026-09-11)

- `/setup` (steward/admin) — 회사명·매장(코드=이름=채널)·품목(코드=이름)·
  코드 별칭(영역,현장표기,표준코드)을 화면에서 편집. 저장 시 profile.json 갱신 +
  **코드 사전(code_dictionary)에 별칭 즉시 반영** — 저장 직후 헤더 회사명이
  바뀌는 것까지 실측(테스트). 형식 오류는 몇 행이 왜 틀렸는지 한글로 반려(400).
- 온보딩 어휘 워크숍(12주 계획 W3~4)의 산출물을 현장에서 직접 입력하는 관문 —
  신규 테넌트가 "화면만으로 첫 브리핑 도달"(W6 DoD)에 필요한 마지막 조각.
- 테스트 3종 — 전체 74+3skip(sqlite)·77(PG) green.

## 셀프서비스 전체 여정 리허설 — W6 DoD (2026-09-11)

새 테넌트가 **화면만으로 첫 브리핑에 도달**하는지 전 구간을 이어서 실측:

1. `tenant create journey` (1초) → 2. 발급 계정 로그인(303)
3. `/setup` 온보딩 저장 — 매장 2·품목 2·별칭 1, 200 '저장 완료'
4. `/upload` POS 정산 cp949 파일 업로드 — '반입 완료' 2행
5. 야간 사이클 `--once` — **전 단계 정상**, 판단 카드 #51 → 발주 초안 PO/AXP/00024 자동 생성
6. `/briefing` — 개명한 회사명(여정검증제과)으로 렌더

업로드 행의 사실 테이블 도달 확인: S-NAMPO/P-BREAD 70(순수 업로드분),
S-CHORYANG/P-PIE 318(템플릿+업로드 110 합산). 테넌트는 검증 후 파기.

**판정**: 12주 계획 W6 DoD("신규 테넌트가 화면만으로 첫 브리핑 도달")를
이 환경 조건에서 충족. 남은 것은 실서버 공개(P4-7 가이드 실행)뿐.

## CDC 소크 누적 (P4 구간)

| 사이클 | 시각(UTC) | 정합 |
|---|---|---|
| 5 | 09-11 21:32 | 9계열 전부 일치 (뷰 16/6/1) |

## P4-10: 계정 관리 화면 (2026-09-11)

- `/users` (admin 전용) — 사용자 목록(역할·상태: 정상/초기 비밀번호/잠금 중),
  **계정 추가**(임시 비밀번호를 화면에 딱 한 번 표시 — '봉투' 규약의 화면판),
  **임시 비밀번호 재발급**(기존 비밀번호 즉시 무효 + 첫 로그인 변경 강제),
  **잠금 해제**(로그인 실패 카운터 소거). 모든 동작 감사 경보 기록.
- 승인제 체험 신청의 운영 동선 완성: 신청 메일 수신 → /users에서 계정 생성 →
  임시 비밀번호 회신 → 사용자는 첫 로그인에서 변경.
- 실측 테스트: 추가→임시 비번 로그인→재발급 후 구비번 401·신비번 303→
  5회 실패 잠금 423→관리자 해제→즉시 303 전 흐름. 아이디 형식·중복 400.
- 전체 76+3skip(sqlite)·79(PG) green.

## CDC 소크 누적 (계속)

| 6 | 09-11 21:36 | 9계열 전부 일치 (지연 176 bytes — 사실상 0) |

## P4-11: 업로드 직후 반입 미리보기 (2026-09-11)

- 반입 성공 화면에 즉시 요약 표시 — 기간·매장 수·상품 종수·수량 합계.
  체험 고객이 "내 파일이 제대로 읽혔나"를 다음 배치 전에 바로 확인한다.
- 미리보기 실패는 반입 성공을 가리지 않는다(예외 무해화). 브리핑 반영 시점
  안내 문구 추가. 테스트 1종 — 전체 77+3skip(sqlite)·80(PG) green.

## P4-12: 수동 배치 실행 화면 (2026-09-11)

- `/runs` (admin 전용) — 야간 배치를 버튼 한 번으로 백그라운드 실행:
  실행 이력 테이블(batch_runs)에 상태(실행 중/완료/실패)·요약·시각 기록,
  **동시 실행 차단(409)**, 기준일 검증. 실패 시 어느 단계가 실패했는지 요약.
- **라이브 실측**: 체험 테넌트에서 웹 버튼 실행 → 90초 후 '완료 · 전 단계 정상'
  이력 확인. 업로드→배치→브리핑이 야간을 기다리지 않고 한 세션에서 끝난다 —
  체험 UX의 마지막 조각.
- 테스트 1종(권한·완료/실패 기록·동시 차단·날짜 검증) —
  전체 78+3skip(sqlite)·81(PG) green.

## 배포 패키지 정합 (2026-09-11)

- install-app-server.sh에 하트비트 타이머 등록 추가(P4-4 결선), 완료 안내에
  Tunnel·체험 운영 크론 경로 명시. `deploy/crontab.example` 신설 —
  0시 리셋·24h 파기·`--no-subscriptions` 백업(P2-I9)을 복붙 가능한 형태로.

## 부하 재검 — 신규 화면 반영 후 (2026-09-11)

P4에서 웹앱에 화면 6종(업로드·온보딩·계정·배치·연결·상태)과 매 렌더
프로파일 로드가 추가된 뒤 회귀 확인: 동시 10 p95 162ms · 동시 20 p95 306ms,
600요청 무오류 — P3-4 기준선(167/319ms)과 동등. 회귀 없음.

## 4단계 완결 판정 (2026-09-11)

**판정: 이 환경에서 실행 가능한 4단계 전 항목(P4-1~P4-12) 완결.**

- 소프트웨어: 화면 6종 신설·테넌트 도구·보안·감시 — 테스트 81종(PG) 양쪽 green,
  부하 회귀 없음, CDC 소크 7사이클 무경보, 셀프서비스 여정(W6 DoD) 완주.
- 실행 준비물: Tunnel 스크립트·배포 가이드·크론 예시·약관 초안 — 서버 실행만 남음
  (5단계 계획서 2장 '사용자 처리 목록'으로 이관).
- 산출물(docs/presentations/): AX_P4_Final_Report.docx(종합결과레포트, 구성도 포함) ·
  AX_P5_Master_Plan.docx(5단계 실행계획서) · AX_Operations_Manual_v2.pptx(표준
  운영매뉴얼 24쪽, 화면 16종) · AX_HW/SW_Architecture.png(구성도 원본).
- 저장소 상태: 브랜치 claude/odoo-ai-erp-presentation-ymviq8, 전부 푸시·미커밋 0.

## 재시작 내성 실증 (2026-09-11 22:12Z)

실행 환경 재시작으로 PG가 내려갔다 올라온 뒤(`service postgresql start`),
**논리 복제 구독이 자동 재개**되고 소크 8사이클도 전 계열 일치(슬롯 지연 12kB).
정전·재부팅 시나리오에서 CDC가 사람 개입 없이 복구됨을 실측 — 운영 안심 요소.

## P5-S: 공개 전 보안 보강 (2026-09-11, 2시간 루프 1)

5단계 공개(try.odooaierp.com)를 앞두고 웹앱을 공개 인터넷 수준으로 보강:

- **S1 세션 만료**: 쿠키 토큰에 만료 시각 포함(기본 12h, AXP_SESSION_HOURS),
  만료·위조 시 재로그인. prod 모드에선 secure 쿠키.
- **S2 초기 비밀번호 강제 변경**: must_change 상태면 /password 외 전 화면
  리다이렉트 — 배너 안내에서 강제로 격상. 8자 미만 거절.
- **S3 CSRF 중앙 강제**: 미들웨어가 로그인 제외 전 POST의 토큰을 검사
  (multipart 업로드 포함 — 본문 버퍼·재생), page()가 모든 POST 폼에 토큰
  자동 주입 — 새 폼을 만들어도 자동 방어. 토큰 누락 403 실측.
- **S4 보안 헤더**: X-Frame-Options SAMEORIGIN · nosniff · Referrer-Policy ·
  CSP(default-src 'self') 전 응답 부착.
- 테스트 4종 추가 + 기존 전 테스트를 토큰 자동 주입 클라이언트로 이행 —
  **82+3skip(sqlite)·85(PG) green**.

## P5-S5: 백업/복구 드릴 자동화 (2026-09-11, 2시간 루프 1)

`deploy/backup-drill.sh` — SLA "월 1회 복구 리허설"의 원커맨드화:
백업(--no-subscriptions)→임시 DB 복원→**전 테이블 건수 전수 대조**→정리,
성공/실패 모두 플랫폼 경보로 기록(실패 crit 실측 확인).

**드릴이 첫 실행에서 실버그 적발(P5-I1)**: prod 경로에서 dim_equipment가
staging_mrp만 보는데, 실 Odoo에선 설비가 정비(staging_maintenance)에서 온다 —
정비 사실 전부가 고아(FK 복원 실패로 발각). 두 원천 합집합 + NULL 제외로 수정,
재변환 후 고아 0 확인, 드릴 재실행 **DRILL OK — 112테이블 일치·총 12초**.
worker 차원의 NULL 방어도 함께. 테스트 82+3skip(sqlite)·85(PG) green.

| 번호 | 문제 | 처리 |
|---|---|---|
| P5-I1 | 실 Odoo 경로에서 정비 사실의 설비가 차원에 없어 고아 — demo에선 mrp에 늘 설비가 있어 숨어 있던 결함. 복원 드릴의 FK 재검증이 발각 | dim_equipment를 mrp+maintenance 합집합으로, NULL 설비·작업자 제외 (**해결**) |

## P5-S6: 내부 API 공유 키 (2026-09-11, 2시간 루프 1)

- axp-api에 AXP_API_KEY 옵션 — 설정 시 전 요청 X-API-Key 요구(/health 제외),
  미설정이면 기존 사내망 신뢰 유지. 테스트 2종(강제/개방).
- Odoo 애드온이 파라미터(axp_inbox.api_key)로 키를 전송하도록 3개 호출 갱신.
- Caddyfile에 신뢰 경계 주석(공개 Tunnel엔 /api 금지 — 실제로 install-tunnel.sh는
  webapp만 노출), crontab.example에 월 1회 복구 드릴 추가.
- 전체 84+3skip(sqlite)·87(PG) green. 소크 9사이클 무경보 지속.

## 보안 자체 리뷰와 수정 5건 (2026-09-11, 2시간 루프 2)

공개 전 마지막 관문으로 전체 코드 보안 리뷰(자동 식별 패스)를 수행 —
Medium 5건 발견, **전부 즉시 수정**:

| 번호 | 발견 | 수정 |
|---|---|---|
| SEC1 | War Room 저장 XSS — 반려 사유 등 DB 유래 값이 이스케이프 없이 렌더(사유는 사용자 입력에서 유래), CSP에 form-action 부재 | render 전 값 전부 escape + 반려 사유 서버측 화이트리스트 강제 + CSP form-action 'self'·base-uri 'none' |
| SEC2 | 세션 폐기 불가 — 비밀번호 변경·재발급 후에도 도난 쿠키가 12h 유효 | 세션 서명에 비밀번호 해시 조각 바인딩 — 변경 즉시 전 세션 무효(테스트로 도난 쿠키 축출 실증) |
| SEC3 | AXP_SECRET 미설정 시 하드코딩 폴백 — 운영에서 뜨면 세션·CSRF 전면 위조 가능 | prod 모드에서 미설정이면 기동 거부(페일 클로즈드) |
| SEC4 | 세션/CSRF 서명의 도메인 미분리 + 순수 숫자 아이디로 교차 해석 여지 | 'sess|'/'csrf|…|form' 접두 분리 + 숫자 전용 아이디 금지 |
| SEC5 | GET /logout — 외부 페이지가 강제 로그아웃 유발 가능 | 로그아웃 POST+CSRF화(GET은 무동작), 헤더를 인라인 폼으로 |

SQL 주입·경로 탐색·기타 XSS·pickle 등은 점검 결과 비해당(전부 파라미터화·
이스케이프·화이트리스트 확인). 테스트 5종 추가 — **89+3skip(sqlite)·92(PG) green**.

## 보안 강화 후 여정 재검증 + 문서 정합 (2026-09-11, 2시간 루프 2)

- **여정 재검증(회귀)**: 새 테넌트 → 로그인 → **강제 /password 리다이렉트 실측**
  → 변경(CSRF 토큰 추출 포함) → 구세션 무효 확인 → 재로그인 → 업로드 '반입 완료'.
  보안 강화가 체험 흐름을 깨지 않음을 curl 수준(실브라우저 동작)으로 확인.
- 배포 가이드(5장)와 운영매뉴얼 v2 공통 슬라이드를 새 정책(강제 변경·12h 세션·
  변경 시 전 기기 재로그인)으로 갱신 — 재빌드 24쪽.
- CDC 소크 12사이클 연속 무경보.

## P5-U1: 감사 로그 검색·필터 + 부하 최종 재검 + CI 정합 (2026-09-11, 2시간 루프 2)

- **/audit 검색**: 카드 번호·행위(동적 목록)·기간(7/30일) 필터, 카드 번호 링크로
  해당 카드 이력만 바로 — "왜 이 발주가 나갔나"의 운영 동선. 테스트 1종.
- **부하 재검(보안 미들웨어 이후)**: 동시 20 p95 347ms(기준선 306~319ms 대비
  +10% — 세션 서명에 비밀번호 해시 바인딩 조회 비용) · 400요청 무오류.
  SLO(1초) 대비 여유 3배 유지 — 수용.
- **CI 정합**: python-multipart 누락 발견·추가(웹앱 폼 테스트가 CI에서 깨질 뻔).
- 전체 90+3skip(sqlite)·93(PG) green.

## P5-U2: 격리 큐 일괄 처리 (2026-09-11, 2시간 루프 3)

- **일괄 확정**: '번호=코드' 줄 단위 텍스트로 여러 건을 한 번에 —
  틀린 줄은 몇 행이 왜 틀렸는지 한글로 알려주고 나머지는 처리(테스트: 2건
  확정+1건 거절 동시). 온보딩 초기, 격리가 수십 건 쌓일 때의 운영 마찰 해소.
- **자기 일치 자동 확정 버튼**: 별칭이 표준 코드와 완전 일치하는 대기 건을
  원클릭 정리(기존 drain_self_matches 결선). 모든 동작 경보 기록.
- 테스트 2종 — 전체 92+3skip(sqlite)·95(PG) green.

## 데모 상태 스냅샷 보존 (2026-09-11, 2시간 루프 3)

- **발견**: customers/*/out은 .gitignore라 9/14 데모 실행 상태(대기 카드 5건 포함
  95MB axp.db)가 로컬 전용 — 컨테이너 소멸 시 유실 위험. 영상·샘플 zip은
  이미 git 보존 확인.
- **조치**: 압축 스냅샷(18MB) + RESTORE.md(복원 3줄)를 커밋. 임시 복원으로
  **대기 카드 #42·43·44·46·47 그대로 살아있음을 검증** 후 보존 —
  이제 어떤 환경에서도 9/14 데모 상태를 3줄로 되살릴 수 있다.
- 참고: CI 회귀 30선 자동화는 데이터가 CI에 없어 보류(로컬·서버 절차 유지) —
  이번 루프에서 수동 재확인 30/30.

## 2시간 무한루프 종합 (2026-09-11 22:14~22:52Z 구간 실적)

| 구분 | 내용 |
|---|---|
| 보안 | P5-S 4종(세션 만료·강제 변경·CSRF·헤더) + 자체 보안 리뷰 Medium 5건 전부 수정(SEC1~5) + 내부 API 키(P5-S6) |
| 운영 도구 | 복구 드릴 원커맨드(112테이블 12초, 실버그 P5-I1 적발·수정) · 감사 로그 검색(P5-U1) · 격리 큐 일괄 확정(P5-U2) |
| 검증 | 여정 재검증(강제 변경 흐름) · 부하 재검(p95 347ms) · 회귀 30/30 · CDC 소크 14사이클 무경보(재시작 내성 포함) |
| 보존 | 데모 상태 스냅샷 git 보존·복원 검증 · CI 의존성 결손(python-multipart) 수정 |
| 품질 | 테스트 81 → **92+3skip(sqlite)·95(PG)** 전부 green · 커밋 12건 전부 푸시 |

## P5-Q: CI 보강 + 모바일 반응형 점검 (2026-09-11 23:2x, 연장 루프)

- **CI에 회귀 30선 편입**: 데모 스냅샷(git 보존분)을 CI에서 풀어 판단 조립기
  회귀 30선을 매 푸시마다 실행 — 로컬 재현 30/30. 이제 인용 강제·환각 차단
  회귀가 CI 게이트가 됐다(이전엔 수동).
- **애드온 검증 단계**: odoo-addon 전 .py 컴파일 + .xml 정형성 검사(2파일) —
  Odoo 설치 없이 잡을 수 있는 결함은 CI에서 차단.
- **모바일(400px) 점검**: 로그인·승인함·업로드·브리핑·계정·상태 6화면 캡처 —
  가로 스크롤 0(scrollWidth=400), 내비 줄바꿈·카드 스택·폼 전폭 정상.
  체험 고객의 휴대폰 접속(Tunnel 공개 후) 대응 확인. 수정 필요 없음.
- CDC 소크 16사이클 무경보 지속(재기동 3회째 자동 재개).

## P5-D: 2차 범위 선행 — 로트 추적·작업장 매핑 실증 (2026-09-11 23:3x)

3단계에서 "2차 범위"로 미뤘던 두 항목을 실 Odoo로 완성:

- **복제 확장**: 발행 후보에 stock_move_line·stock_lot·mrp_workcenter 추가
  (odoo_cdc_prod.sql) → 실 인스턴스에 ALTER PUBLICATION + 구독 REFRESH —
  14릴레이션 전부 ready.
- **로트 추적**: 실 Odoo에서 로트 관리 원자재(M-FLOUR-T) 발주→로트 입고
  (WH/IN/00001, LOT-2609-A) → v_stock_move가 move_line×stock_lot 조인으로
  로트 투영 → **sync_prod 후 staging_stock_move.lot_id='LOT-2609-A' 실측**.
  이제 fact_procurement의 'LOT-미상'이 입고 확정 시 실로트로 채워지는 경로 개통.
- **작업장 매핑**: 실 BOM(공정 '굽기'=OVEN-2)로 제조지시 WH/MO/00002 생성·진행
  → v_mrp가 workorder×workcenter 조인으로 line_id=2·equipment_id=OVEN-2 투영
  → **staging_mrp까지 도달 실측**. 설비 예지·불량 상관의 실데이터 연결 고리 완성.
- 적용 중 잡은 것: mrp_workcenter.name은 varchar(jsonb 아님) — ::text 캐스트.
- 전체 92+3skip(sqlite)·95(PG) green · 소크 17사이클 무경보.

## P5-D 후속: 로트 차원 합류 + 실 MO 사실 적재 (2026-09-11 23:4x)

- **로트 차원 합류**: dim_material_lot을 조달(staging_purchase)과 입고 무브
  (staging_stock_move의 실로트) 합집합으로 구성 — 재변환에서
  **LOT-2609-A가 차원에 실합류** 확인. 'LOT-미상' 표식 → 입고 후 backfill로
  실로트 연결되는 경로가 코드로 완성.
- **P5-I2 실버그**: 실 Odoo MO엔 교대(shift)가 없어 fact_production NOT NULL
  붕괴(P3-I9와 동류 — demo 데이터엔 늘 shift가 있어 숨어 있었음) →
  '미상' 표식 적재로 수정, WH/MO/00002가 OVEN-2·qty 100으로 사실 테이블 도달.
- prod 모드 전 구간 사이클 재확인 — **전 단계 정상**. 92+3skip·95(PG) green.

| 번호 | 문제 | 처리 |
|---|---|---|
| P5-I2 | 실 Odoo MO에 교대 없음 → fact_production NOT NULL 붕괴 | shift '미상' 표식 적재(장표 반입 시 재처리로 채움) (**해결**) |

## P5-D2: 품목 마스터 복제 — 숫자 ID → 사람 코드 (2026-09-11 23:5x)

- **문제**: prod 경로에서 품목이 숫자 id('1','7')로 흘러 브리핑·카드에 숫자가
  노출될 상태였다(demo에선 P- 코드라 안 보이던 격차).
- **해소**: product_product·product_template를 복제에 추가(16릴레이션) →
  헬퍼 뷰 v_product_code(default_code → 번역 이름 → id 폴백) → 판매·구매·재고·
  생산·스크랩 전 뷰가 코드로 투영. **전량 재동기화 후 dim_product =
  [P-BREAD-T, P-CAKE-T, P-PIE-T], 로트 표식도 M-FLOUR-T 기반** 실측.
- 파일 순서 결함(신규 DB에서 스키마 생성 전 DROP) 수정 — 스키마 삭제 후
  단독 적용 재검증. prod 전 구간 사이클 '전 단계 정상' 유지.
## P5-D3: 정비 설비 이름 투영 — 설비 차원 합류 (2026-09-12 00:0x)

- **문제**: v_maintenance가 equipment_id를 숫자 id('1')로 투영해, v_mrp의
  작업장 이름('OVEN-2')과 dim_equipment에서 서로 다른 행으로 분리됐다
  (같은 물리 설비인데 차원이 갈라져 정비 사실이 생산과 못 만남).
- **해소**: maintenance_equipment.name(jsonb, `{"en_US": "OVEN-1(테스트)"}` 실측)
  을 LEFT JOIN해 COALESCE(en_US → 아무 번역 → id 폴백)로 투영 —
  v_product_code와 같은 폴백 규약. 전량 재동기화 실측:
  **dim_equipment = [OVEN-1(테스트), OVEN-2]** (숫자 '1' 소멸),
  fact_equipment_event 19건 전부 이름으로 결선.
- prod 사이클 '전 단계 정상', 테스트 92+3skip(SQLite)/95(PG) 유지.
- **결정(복제 범위)**: res_partner는 이메일·전화 등 PII를 담아 **복제하지
  않는다** — 공급사(vendor_id)는 숫자 id로 유지하고, 사람이 읽을 별칭은
  M2 codemap이 담당(온사이트에서 이름이 필요하면 발행 후보에 추가하되
  개인정보 차단 목록 M1-P를 먼저 적용하는 조건).
- **관찰(게이트 warn 2건, 원인 규명 — 실데이터가 드러낸 차원 커버리지 격차)**:
  - P5-I3: dim_calendar가 판매일자만으로 구성 → 미래 착수 MO(2026-09-16)의
    fact_production.date_key가 참조 고아(warn). 달력을 판매∪생산∪재고∪정비
    일자 합집합으로 확장할 것(다음 블록).
  - P5-I4: dim_product가 판매 품목만 담음 → 자재 입고 무브(M-FLOUR-T)의
    fact_inventory_move.product_id가 참조 고아(warn). 무브 품목 검증을
    dim_product∪dim_material 합집합 기준으로 바꾸거나 차원 포함 범위를
    재정의할 것(다음 블록).

## P5-I3·I4 해소: 차원 커버리지 격차 (2026-09-12 00:1x)

- **P5-I3 해소**: dim_calendar를 판매 단독 → 판매∪생산∪조달∪재고∪품질∪정비
  일자 합집합으로 확장. 미래 착수 MO(2026-09-16)의 date_key 참조 고아 소멸.
- **P5-I4 해소**: 재고 이동·생산에만 나타나는 품목을 dim_product에 합류 —
  조달 자재 목록에 있으면 category='material', 아니면 'bakery'.
  prod 실측: dim_product = [M-FLOUR-T(material), P-BREAD-T, P-CAKE-T, P-PIE-T].
- **prod 품질 게이트 위반 0 달성**(직전 warn 2건 소멸) — daily_report ok=True.
- 테스트 92+3skip(SQLite)/95(PG), 데모 스냅샷 회귀 30/30 유지
  (데모 프로파일은 무브 품목이 전부 판매 품목이라 차원 내용 불변).
- CDC 소크 19사이클 정합(all_ok, mismatch 없음, slot lag 490kB 정상 범위).
