"""로봇 LMS 팩 (R01~) — 학습자는 사람이 아니라 로봇이다.

핵심 통찰(제안서 v2): 초등~대학 교과 지식은 LLM에 이미 사전학습되어 있다.
그러므로 이 팩은 '가르치는 시스템'이 아니라
① 아는지 검정하고(로봇 검정고시, R01)
② 모르는 것을 보정하고
③ 고객이 원하는 분야만 추가 학습시키는(분야팩, R03~) 시스템이다.

로봇의 지능 = L1 기반지능 × L2 분야지능 × L3 주인지능(브레인그래프).
"""
import env_loader

env_loader.load()
